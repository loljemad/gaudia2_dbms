DROP DATABASE gym;
CREATE DATABASE gym;

USE gym;

CREATE TABLE client (
client_id int PRIMARY KEY AUTO_INCREMENT,
fname varchar(255),
lname varchar(255),
gender ENUM ('female', 'male') DEFAULT 'male',
age int,
contact_add varchar(255),
email varchar(255),
password varchar (255)
);

CREATE TABLE transaction_records (
trans_id INT PRIMARY KEY AUTO_INCREMENT,
trans_name varchar(255),
client_id INT,
amount int,
datee DATE,
CONSTRAINT FKclientID FOREIGN KEY(client_id)
REFERENCES client(client_id)
);

CREATE TABLE reports (
report_id INT PRIMARY KEY AUTO_INCREMENT,
total_amount varchar(255),
datee DATE,
client_id int,
trans_id int,
 CONSTRAINT fkreportclientID FOREIGN KEY (client_id)
 REFERENCES client(client_id),
 CONSTRAINT FKtransID FOREIGN KEY (trans_id)
 REFERENCES transaction_records(trans_id)
);

CREATE TABLE schedule (
sched_id INT PRIMARY KEY AUTO_INCREMENT,
client_id INT,
trainor_id INT,
schedule_session varchar(255),
activitiy varchar(255),
datee DATE,
time_start INT,
time_end INT

);

CREATE TABLE trainor (
trainor_ID INT PRIMARY KEY AUTO_INCREMENT,
trainor_name varchar(255),

salary INT,
email varchar(255),
trainor_password varchar(255)

);

CREATE TABLE membership (
mem_id INT PRIMARY KEY AUTO_INCREMENT,
client_id INT,
mem_status varchar(255),
datee DATE
);

CREATE TABLE payment (
payment_id INT PRIMARY KEY AUTO_INCREMENT,
client_id INT,
datee DATE,
amount INT

);




-- INSERT INTO client(fname,lname)VALUES ('Jack', 'Frost');
-- INSERT INTO schedule(schedule_session,activity,datee,time_start) VALUES('bruh', 'moments', '8/19/2001', '8:15')


-- SELECT fname,lname FROM client INNER JOIN schedule ON schedule_session, activity, datee, time_start;







-- SELECT  CONCAT(client.fname, ' ', client.lname) AS Full_Name,  schedule.schedule_session AS Session,schedule.activitiy AS Activity,schedule.datee AS Date,schedule.time_start AS Time_Start,trainor.trainor_name AS Name_of_Instructor
-- FROM  client INNER JOIN schedule ON client.client_id = schedule.client_id INNER JOIN trainor ON schedule.trainor_id = trainor.trainor_ID;
